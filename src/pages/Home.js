import React from 'react';
import Maincontent from '../maincontent';
import Header from '../header';
function Home(props) {
    return (
        <>
        <Header/>
        <Maincontent>
        </Maincontent>
        </>
    );
}

export default Home;